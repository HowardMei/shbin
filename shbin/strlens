
#%   DESCRIPTION:  Get the length of the given strings
#%   USAGE: echo "strings" | strlens OR strlens "strings"
#%   REQUIRES: strlen

if [ $# -lt 1 ]; then
    echo "$(cat)" | strlen
else
    echo "$*" | strlen
fi

exit 0
